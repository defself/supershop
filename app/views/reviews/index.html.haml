%h2 Reviews

%table
  %thead
    %tr
      %th Msg
      - if admin?
        %th
        %th

  %tbody
    - @reviews.each do |review|
      %tr
        %td= review.msg
        - if admin?
          %td= link_to 'Edit', edit_product_review_path(@product, review)
          %td= link_to 'Destroy', product_review_path(@product, review), method: :delete, data: { confirm: 'Are you sure?' }
